{
  "name": "annotations-api",
  "version": "1.0.0",
  "main": "dist/main.js",
  "types": "dist/main.ts",
  "license": "MPL-2.0",
  "private": true,
  "scripts": {
    "build:dev": "rm -rf dist && NODE_ENV=development npm run synth",
    "synth": "cdktf synth",
    "compile": "tsc --pretty",
    "watch": "tsc -w",
    "test": "echo ok"
  },
  "dependencies": {
    "@pocket-tools/terraform-modules": "3.13.4"
  },
  "devDependencies": {
    "@types/node": "16.11.35",
    "@pocket-tools/tsconfig": "1.0.1"
  }
}
